---
title: 프록시클라이언트MaxBodySize
description: 프록시를 사용할 때 최대 요청 본문 크기를 구성합니다.
version: experimental
---

프록시가 사용되면 Next.js는 요청 본문을 자동으로 복제하고 이를 메모리에 버퍼링하여 프록시와 기본 경로 핸들러 모두에서 다중 읽기를 활성화합니다. 과도한 메모리 사용을 방지하기 위해 이 구성 옵션은 버퍼링된 본문에 크기 제한을 설정합니다.

기본적으로 최대 본문 크기는 **10MB**입니다. 요청 본문이 이 제한을 초과하면 본문은 제한까지만 버퍼링되며 제한을 초과한 경로를 나타내는 경고가 기록됩니다.

## 옵션

### 문자열 형식(권장)

사람이 읽을 수 있는 문자열 형식을 사용하여 크기를 지정합니다.

```ts filename="next.config.ts" switcher
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  experimental: {
    proxyClientMaxBodySize: '1mb',
  },
}

export default nextConfig
```

```js filename="next.config.js" switcher
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    proxyClientMaxBodySize: '1mb',
  },
}

module.exports = nextConfig
```

지원되는 단위: `b`, `kb`, `mb`, `gb`

### 숫자 형식

또는 크기(바이트)를 숫자로 지정하십시오.

```ts filename="next.config.ts" switcher
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  experimental: {
    proxyClientMaxBodySize: 1048576, // 1MB in bytes
  },
}

export default nextConfig
```

```js filename="next.config.js" switcher
/** @type {import('next').NextConfig} */
const nextConfig = {
  experimental: {
    proxyClientMaxBodySize: 1048576, // 1MB in bytes
  },
}

module.exports = nextConfig
```

## 행동

요청 본문이 구성된 제한을 초과하는 경우:

1. Next.js는 처음 N 바이트만 버퍼링합니다(최대 한도까지).
1. 제한을 초과한 경로를 나타내는 경고가 콘솔에 기록됩니다.
1. 요청은 정상적으로 계속 처리되지만 본문의 일부만 사용할 수 있습니다.
1. 요청이 실패하지 않거나 클라이언트에 오류를 반환하지 **않습니다**

애플리케이션이 전체 요청 본문을 처리해야 하는 경우 다음 중 하나를 수행해야 합니다.

- `proxyClientMaxBodySize` 제한을 늘립니다.
- 애플리케이션 로직에서 부분 본문을 적절하게 처리합니다.

## 예

```ts filename="proxy.ts"
import { NextRequest, NextResponse } from 'next/server'

export async function proxy(request: NextRequest) {
  // Next.js automatically buffers the body with the configured size limit
  // You can read the body in proxy...
  const body = await request.text()

  // If the body exceeded the limit, only partial data will be available
  console.log('Body size:', body.length)

  return NextResponse.next()
}
```

```ts filename="app/api/upload/route.ts"
import { NextRequest, NextResponse } from 'next/server'

export async function POST(request: NextRequest) {
  // ...and the body is still available in your route handler
  const body = await request.text()

  console.log('Body in route handler:', body.length)

  return NextResponse.json({ received: body.length })
}
```

## 알아두면 좋은 점

- 이 설정은 애플리케이션에서 프록시가 사용되는 경우에만 적용됩니다.
- 10MB의 기본 제한은 메모리 사용량과 일반적인 사용 사례의 균형을 맞추기 위해 설계되었습니다.
- 한도는 모든 동시 요청에 전역적으로 적용되는 것이 아니라 요청별로 적용됩니다.
- 대용량 파일 업로드를 처리하는 애플리케이션의 경우 그에 따라 제한을 늘리는 것이 좋습니다.
