---
title: 사용자 정의 웹팩 구성
nav_title: 웹팩
description: Next.js에서 사용하는 webpack 구성을 사용자 정의하는 방법을 알아보세요.
---

> **알아두면 좋은 정보**: webpack 구성에 대한 변경 사항은 semver에서 다루지 않으므로 위험은 본인 부담으로 진행하세요.

애플리케이션에 사용자 정의 웹팩 구성을 계속 추가하기 전에 Next.js가 이미 사용 사례를 지원하지 않는지 확인하세요.

- [CSS 수입](../../../getting-started/css.mdx)
- [CSS 모듈](../../../getting-started/css.mdx#css-modules)
- [Sass/SCSS 가져오기](../../../guides/sass.mdx)
- [Sass/SCSS 모듈](../../../guides/sass.mdx)

일반적으로 요청되는 일부 기능은 플러그인으로 제공됩니다.

- [@next/mdx](https://github.com/vercel/next.js/tree/canary/packages/next-mdx)
- [@next/bundle-analyzer](https://github.com/vercel/next.js/tree/canary/packages/next-bundle-analyzer)

`webpack`의 사용을 확장하려면 다음과 같이 `next.config.js` 내에서 해당 구성을 확장하는 함수를 정의할 수 있습니다.

```js filename="next.config.js"
module.exports = {
  webpack: (
    config,
    { buildId, dev, isServer, defaultLoaders, nextRuntime, webpack }
  ) => {
    // Important: return the modified config
    return config
  },
}
```

> `webpack` 함수는 서버(nodejs/edge 런타임)에 대해 두 번, 클라이언트에 대해 한 번, 총 세 번 실행됩니다. 이를 통해 `isServer` 속성을 사용하여 클라이언트와 서버 구성을 구별할 수 있습니다.

`webpack` 함수의 두 번째 인수는 다음 속성을 가진 개체입니다.

- `buildId`: `String` - 빌드 간 고유 식별자로 사용되는 빌드 ID입니다.
- `dev`: `Boolean` - 개발 중에 컴파일이 수행되는지 여부를 나타냅니다.
- `isServer`: `Boolean` - 서버측 컴파일의 경우 `true`이고, 클라이언트측 컴파일의 경우 `false`입니다.
- `nextRuntime`: `String | undefined` - 서버 측 컴파일을 위한 대상 런타임입니다. `"edge"` 또는 `"nodejs"` 중 하나이며 클라이언트 측 컴파일의 경우 `undefined`입니다.
- `defaultLoaders`: `Object` - Next.js에서 내부적으로 사용하는 기본 로더:
  - `babel`: `Object` - 기본 `babel-loader` 구성.

`defaultLoaders.babel` 사용 예:

```js
// Example config for adding a loader that depends on babel-loader
// This source was taken from the @next/mdx plugin source:
// https://github.com/vercel/next.js/tree/canary/packages/next-mdx
module.exports = {
  webpack: (config, options) => {
    config.module.rules.push({
      test: /\.mdx/,
      use: [
        options.defaultLoaders.babel,
        {
          loader: '@mdx-js/loader',
          options: pluginOptions.options,
        },
      ],
    })

    return config
  },
}
```

#### `nextRuntime`

`isServer`이 `true` 또는 `nextRuntime`인 경우 `"edge"`은 `"nodejs"`입니다. `nextRuntime` `"edge"`은 현재 Edge 런타임의 프록시 및 서버 구성 요소에만 사용됩니다.
