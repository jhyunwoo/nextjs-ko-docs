---
title: 사이트맵 생성
nav_title: 사이트맵 생성
description: generateSiteMaps 함수를 사용하여 애플리케이션에 대한 여러 사이트맵을 만드는 방법을 알아보세요.
related:
  title: 다음 단계
  description: Next.js 애플리케이션용 사이트맵을 만드는 방법을 알아보세요.
  links:
    - app/api-reference/file-conventions/metadata/sitemap
---

You can use the `generateSitemaps` 함수를 사용하여 애플리케이션에 대한 여러 사이트맵을 생성할 수 있습니다.

## 반품

The `generateSitemaps`은 `id` 속성이 있는 객체 배열을 반환합니다.

## URL

생성된 사이트맵은 `/.../sitemap/[id].xml`에서 사용할 수 있습니다. 예를 들어 `/product/sitemap/1.xml`입니다.

## 예

예를 들어 `generateSitemaps`을 사용하여 사이트맵을 분할하려면 사이트맵 `id`이 포함된 개체 배열을 반환합니다. 그런 다음 `id`를 사용하여 고유한 사이트맵을 생성하세요.

```ts filename="app/product/sitemap.ts" switcher
import type { MetadataRoute } from 'next'
import { BASE_URL } from '@/app/lib/constants'

export async function generateSitemaps() {
  // Fetch the total number of products and calculate the number of sitemaps needed
  return [{ id: 0 }, { id: 1 }, { id: 2 }, { id: 3 }]
}

export default async function sitemap(props: {
  id: Promise<string>
}): Promise<MetadataRoute.Sitemap> {
  const id = await props.id
  // Google's limit is 50,000 URLs per sitemap
  const start = id * 50000
  const end = start + 50000
  const products = await getProducts(
    `SELECT id, date FROM products WHERE id BETWEEN ${start} AND ${end}`
  )
  return products.map((product) => ({
    url: `${BASE_URL}/product/${product.id}`,
    lastModified: product.date,
  }))
}
```

```js filename="app/product/sitemap.js" switcher
import { BASE_URL } from '@/app/lib/constants'

export async function generateSitemaps() {
  // Fetch the total number of products and calculate the number of sitemaps needed
  return [{ id: 0 }, { id: 1 }, { id: 2 }, { id: 3 }]
}

export default async function sitemap(props) {
  const id = await props.id
  // Google's limit is 50,000 URLs per sitemap
  const start = id * 50000
  const end = start + 50000
  const products = await getProducts(
    `SELECT id, date FROM products WHERE id BETWEEN ${start} AND ${end}`
  )
  return products.map((product) => ({
    url: `${BASE_URL}/product/${product.id}`,
    lastModified: product.date,
  }))
}
```

## 버전 기록

| 버전 | 변경사항 |`v16.0.0` | The `id` values returned from `generateSitemaps` are now passed as a promise that resolves to a `string` to the sitemap function.                    |`v15.0.0` | `generateSitemaps` now generates consistent URLs between development and production                                                                  |
| `v13.3.2` | `generateSitemaps` introduced. In development, you can view the generated sitemap on `/.../sitemap.xml/[id]`. For example, `/product/sitemap.xml/1`. |
