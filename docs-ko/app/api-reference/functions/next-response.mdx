---
title: 다음응답
description: NextResponse에 대한 API 참조입니다.
---

NextResponse는 추가 편의 메서드를 사용하여 [웹 응답 API](https://developer.mozilla.org/docs/Web/API/Response)을 확장합니다.

## `cookies`

응답의 [`Set-Cookie`](https://developer.mozilla.org/docs/Web/HTTP/Headers/Set-Cookie) 헤더를 읽거나 변경합니다.

### `set(name, value)`

이름이 주어지면 응답에 주어진 값으로 쿠키를 설정하십시오.

```ts
// Given incoming request /home
let response = NextResponse.next()
// Set a cookie to hide the banner
response.cookies.set('show-banner', 'false')
// Response will have a `Set-Cookie:show-banner=false;path=/home` header
return response
```

### `get(name)`

쿠키 이름이 주어지면 쿠키 값을 반환합니다. 쿠키가 발견되지 않으면 `undefined`이 반환됩니다. 여러 쿠키가 발견되면 첫 번째 쿠키가 반환됩니다.

```ts
// Given incoming request /home
let response = NextResponse.next()
// { name: 'show-banner', value: 'false', Path: '/home' }
response.cookies.get('show-banner')
```

### `getAll()`

쿠키 이름이 주어지면 쿠키 값을 반환합니다. 이름이 지정되지 않으면 응답에 모든 쿠키를 반환합니다.

```ts
// Given incoming request /home
let response = NextResponse.next()
// [
//   { name: 'experiments', value: 'new-pricing-page', Path: '/home' },
//   { name: 'experiments', value: 'winter-launch', Path: '/home' },
// ]
response.cookies.getAll('experiments')
// Alternatively, get all cookies for the response
response.cookies.getAll()
```

### `has(name)`

쿠키 이름이 주어지면 응답에 쿠키가 있으면 `true`을 반환합니다.

```ts
// Given incoming request /home
let response = NextResponse.next()
// Returns true if cookie exists, false if it does not
response.cookies.has('experiments')
```

### `delete(name)`

쿠키 이름이 주어지면 응답에서 쿠키를 삭제합니다.

```ts
// Given incoming request /home
let response = NextResponse.next()
// Returns true for deleted, false if nothing is deleted
response.cookies.delete('experiments')
```

## `json()`

주어진 JSON 본문으로 응답을 생성합니다.

```ts filename="app/api/route.ts" switcher
import { NextResponse } from 'next/server'

export async function GET(request: Request) {
  return NextResponse.json({ error: 'Internal Server Error' }, { status: 500 })
}
```

```js filename="app/api/route.js" switcher
import { NextResponse } from 'next/server'

export async function GET(request) {
  return NextResponse.json({ error: 'Internal Server Error' }, { status: 500 })
}
```

## `redirect()`

Produce a response that redirects to a [URL](https://developer.mozilla.org/docs/Web/API/URL)로 리디렉션되는 응답을 생성합니다.

```ts
import { NextResponse } from 'next/server'

return NextResponse.redirect(new URL('/new', request.url))
```

The [URL](https://developer.mozilla.org/docs/Web/API/URL)은 `NextResponse.redirect()` 메서드에서 사용되기 전에 생성 및 수정될 수 있습니다. 예를 들어, `request.nextUrl` 속성을 사용하여 현재 URL를 가져온 다음 이를 수정하여 다른 URL로 리디렉션할 수 있습니다.

```ts
import { NextResponse } from 'next/server'

// Given an incoming request...
const loginUrl = new URL('/login', request.url)
// Add ?from=/incoming-url to the /login URL
loginUrl.searchParams.set('from', request.nextUrl.pathname)
// And redirect to the new URL
return NextResponse.redirect(loginUrl)
```

## `rewrite()`

원본 URL를 유지하면서 지정된 [URL](https://developer.mozilla.org/docs/Web/API/URL)을 다시 작성(프록시)하는 응답을 생성합니다.

```ts
import { NextResponse } from 'next/server'

// Incoming request: /about, browser shows /about
// Rewritten request: /proxy, browser shows /about
return NextResponse.rewrite(new URL('/proxy', request.url))
```

## `next()`

The `next()` 메서드는 일찍 돌아와 라우팅을 계속할 수 있으므로 프록시에 유용합니다.

```ts
import { NextResponse } from 'next/server'

return NextResponse.next()
```

You can also forward `headers`을 사용하여 응답을 생성할 때 `NextResponse.next({ request: { headers } })` 업스트림을 전달할 수도 있습니다.

```ts
import { NextResponse } from 'next/server'

// Given an incoming request...
const newHeaders = new Headers(request.headers)
// Add a new header
newHeaders.set('x-version', '123')
// Forward the modified request headers upstream
return NextResponse.next({
  request: {
    // New request headers
    headers: newHeaders,
  },
})
```

이는 `newHeaders` 업스트림을 대상 페이지, 경로 또는 서버 작업으로 전달하고 이를 클라이언트에 노출시키지 않습니다. 이 패턴은 데이터 업스트림을 전달하는 데 유용하지만 이 데이터가 포함된 헤더가 외부 서비스로 전달될 수 있으므로 주의해서 사용해야 합니다.

대조적으로, `NextResponse.next({ headers })`은 프록시에서 클라이언트로 헤더를 보내는 약어입니다. 이는 **좋은 습관이** 아니며 피해야 합니다. 무엇보다도 `Content-Type`와 같은 응답 헤더를 설정하면 프레임워크 기대치(예: 서버 작업에서 사용되는 `Content-Type`)가 무시되어 제출이 실패하거나 스트리밍 응답이 중단될 수 있기 때문입니다.

```ts
import { type NextRequest, NextResponse } from 'next/server'

async function proxy(request: NextRequest) {
  const headers = await injectAuth(request.headers)
  // DO NOT forward headers like this
  return NextResponse.next({ headers })
}
```

일반적으로 들어오는 요청 헤더를 모두 복사하지 마세요. 그렇게 하면 민감한 데이터가 클라이언트나 업스트림 서비스에 유출될 수 있습니다.허용 목록을 사용하여 수신 요청 헤더의 하위 집합을 생성하는 방어적인 접근 방식을 선호합니다. 예를 들어 사용자 정의  헤더를 삭제하고 알려진 안전 헤더만 전달할 수 있습니다.

Prefer a defensive approach by creating a subset of incoming request headers using an allow-list. For example, you might discard custom `x-*` headers and only forward known-safe headers:

```ts
import { type NextRequest, NextResponse } from 'next/server'

function proxy(request: NextRequest) {
  const incoming = new Headers(request.headers)
  const forwarded = new Headers()

  for (const [name, value] of incoming) {
    const headerName = name.toLowerCase()
    // Keep only known-safe headers, discard custom x-* and other sensitive ones
    if (
      !headerName.startsWith('x-') &&
      headerName !== 'authorization' &&
      headerName !== 'cookie'
    ) {
      // Preserve original header name casing
      forwarded.set(name, value)
    }
  }

  return NextResponse.next({
    request: {
      headers: forwarded,
    },
  })
}
```
