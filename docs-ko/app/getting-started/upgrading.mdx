---
title: 업그레이드 중
description: Next.js 애플리케이션을 최신 버전이나 Canary로 업그레이드하는 방법을 알아보세요.
related:
  title: 버전 가이드
  description: 자세한 업그레이드 지침은 버전 가이드를 참조하세요.
  links:
    - app/guides/upgrading/version-16
    - app/guides/upgrading/version-15
    - app/guides/upgrading/version-14
---

## 최신 버전

Next.js의 최신 버전으로 업데이트하려면 `upgrade` 명령을 사용할 수 있습니다.

```bash filename="Terminal"
next upgrade
```

Next.js 15 및 이전 버전은 `upgrade` 명령을 지원하지 않으며 대신 별도의 패키지를 사용해야 합니다.

```bash filename="Terminal"
npx @next/codemod@canary upgrade latest
```

수동으로 업그레이드하려면 최신 Next.js 및 React 버전을 설치하세요.

```bash package="pnpm"
pnpm i next@latest react@latest react-dom@latest eslint-config-next@latest
```

```bash package="npm"
npm i next@latest react@latest react-dom@latest eslint-config-next@latest
```

```bash package="yarn"
yarn add next@latest react@latest react-dom@latest eslint-config-next@latest
```

```bash package="bun"
bun add next@latest react@latest react-dom@latest eslint-config-next@latest
```

## 카나리아 버전

최신 카나리아로 업데이트하려면 최신 버전의 Next.js를 사용 중이고 모든 것이 예상대로 작동하는지 확인하세요. 그런 다음 다음 명령을 실행하십시오.

```bash filename="Terminal"
npm i next@canary
```

### 카나리아에서 사용할 수 있는 기능

현재 카나리아에서는 다음 기능을 사용할 수 있습니다.

**인증**:

- [`forbidden`](../api-reference/functions/forbidden.mdx)
- [`unauthorized`](../api-reference/functions/unauthorized.mdx)
- [`forbidden.js`](../api-reference/file-conventions/forbidden.mdx)
- [`unauthorized.js`](../api-reference/file-conventions/unauthorized.mdx)
- [`authInterrupts`](../api-reference/config/next-config-js/authInterrupts.mdx)
