---
title: Next.js 애플리케이션에 Tailwind CSS v3를 설치하는 방법
nav_title: 순풍 CSS v3
description: 더 광범위한 브라우저 지원을 위해 Tailwind CSS v3를 사용하여 Next.js 애플리케이션의 스타일을 지정하세요.
---

이 가이드는 Next.js 애플리케이션에 [Tailwind CSS v3](https://v3.tailwindcss.com/)을 설치하는 방법을 안내합니다.

> **알면 좋은 점:** 최신 Tailwind 4 설정은 [Tailwind CSS 설정 지침](../getting-started/css.mdx#tailwind-css)을 참조하세요.

## Tailwind v3 설치

Tailwind CSS 및 해당 피어 종속성을 설치한 후 `init` 명령을 실행하여 `tailwind.config.js` 및 `postcss.config.js` 파일을 모두 생성합니다.

```bash package="pnpm"
pnpm add -D tailwindcss@^3 postcss autoprefixer
npx tailwindcss init -p
```

```bash package="npm"
npm install -D tailwindcss@^3 postcss autoprefixer
npx tailwindcss init -p
```

```bash package="yarn"
yarn add -D tailwindcss@^3 postcss autoprefixer
npx tailwindcss init -p
```

```bash package="bun"
bun add -D tailwindcss@^3 postcss autoprefixer
bunx tailwindcss init -p
```

## Tailwind v3 구성

Configure your template paths in your `tailwind.config.js` 파일에서 템플릿 경로를 구성합니다.

```js filename="tailwind.config.js"
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './app/**/*.{js,ts,jsx,tsx,mdx}',
    './pages/**/*.{js,ts,jsx,tsx,mdx}',
    './components/**/*.{js,ts,jsx,tsx,mdx}',
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
```

전역 CSS 파일에 Tailwind 지시문을 추가합니다.

```css filename="app/globals.css"
@tailwind base;
@tailwind components;
@tailwind utilities;
```

루트 레이아웃에서 CSS 파일을 가져옵니다.

```tsx filename="app/layout.tsx" switcher
import './globals.css'

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
```

```jsx filename="app/layout.js" switcher
import './globals.css'

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  )
}
```

## 클래스 사용하기

Tailwind CSS를 설치하고 전역 스타일을 추가한 후 애플리케이션에서 Tailwind의 유틸리티 클래스를 사용할 수 있습니다.

```tsx filename="app/page.tsx" switcher
export default function Page() {
  return <h1 className="text-3xl font-bold underline">Hello, Next.js!</h1>
}
```

```jsx filename="app/page.js" switcher
export default function Page() {
  return <h1 className="text-3xl font-bold underline">Hello, Next.js!</h1>
}
```

## Turbopack 사용

Next.js 13.1부터 Tailwind CSS 및 PostCSS는 [Turbopack](https://turbo.build/pack/docs/features/css#tailwind-css)에서 지원됩니다.
